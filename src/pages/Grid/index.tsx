interface GridProps {
  // Define any props if needed
}

const Grid = (props: GridProps) => {
  return (
    <div>
      <h1>Grid Page</h1>
      <p>This is the grid page content.</p>
    </div>
  );
};

export default Grid;
